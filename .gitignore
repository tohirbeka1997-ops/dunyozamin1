# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
.env.local
.env.*.local
deploy/deploy.env
scripts/_run_ngrok_both.bat
dist
dist-ssr
output

# Electron packager output (GitHub 100MB limit; build CI/locally)
release/
release2/
release_build/
release_build2/
release_build3/
release_final/
release-livefixes/
vite.config.ts.timestamp-*.mjs
*.local
# package-lock.json — Docker `npm ci` uchun repoda bo‘lishi kerak

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.sync
history/*.json
.vite_cache

# Database files (SECURITY: Never commit DB files)
# All database files should be in userData directory, not in repo
**/*.db
**/*.db-wal
**/*.db-shm
**/*.db-journal
*.db
*.db-wal
*.db-shm
*.db-journal

# Docker / Server mode data
.pos-data
.pos-data-dev
.pos-data-docker
docker-compose.override.yaml

# Load-test artifacts (k6/autocannon) — bulky JSON output, never committed
load-test/results/
load-test/*.local.env

# rclone credentials — kept per-host only, never in repo
deploy/backup/rclone.conf
deploy/monitoring/pos-metrics-token

# Build artifacts & junk — never commit
release.zip
*.prn
=170
package — копия.json
tmp-query.js
tmp-audit-returns.cjs
tmp-*.cjs
test_pos.db*

# Local cleanup / agent artifacts (do not commit)
release-*/
_trash_audit/
__deltest__/
/*.tar.gz
deploy/_eb-*.json
deploy/database-upload-deploy.tar
AUDIT_*.md
FIXES_APPLIED*.md
LIVE_TEST_*.md
TZ_*.md
OPTIMIZATION_*.md
