# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
.env.local
.env.*.local
scripts/_run_ngrok_both.bat
dist
dist-ssr
output

# Electron packager output (GitHub 100MB limit; build CI/locally)
release/
vite.config.ts.timestamp-*.mjs
*.local
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.sync
history/*.json
.vite_cache

# Database files (SECURITY: Never commit DB files)
# All database files should be in userData directory, not in repo
**/*.db
**/*.db-wal
**/*.db-shm
**/*.db-journal
*.db
*.db-wal
*.db-shm
*.db-journal

# Docker / Server mode data
.pos-data
.pos-data-dev
.pos-data-docker
docker-compose.override.yaml

# Load-test artifacts (k6/autocannon) — bulky JSON output, never committed
load-test/results/
load-test/*.local.env

# rclone credentials — kept per-host only, never in repo
deploy/backup/rclone.conf
deploy/monitoring/pos-metrics-token
