# Production build: `npm run build` (Vite .env.production ni o‘qiydi)
#
# Bir domen (tavsiya): Nginx SPA + /v1 → public-api — API URL bo‘sh qoldiring (brauzer xuddi shu hostga /v1 yuboradi)
VITE_PUBLIC_API_URL=

# Yoki alohida API subdomen:
# VITE_PUBLIC_API_URL=https://api.example.com
